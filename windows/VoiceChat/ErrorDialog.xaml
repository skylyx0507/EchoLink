<Window x:Class="VoiceChat.ErrorDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="错误" Height="240" Width="400"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        ResizeMode="NoResize"
        Background="#2b2d31"
        FontFamily="Segoe UI, Microsoft YaHei"
        AllowsTransparency="True">
    <Window.Resources>
        <Style x:Key="DialogBtn" TargetType="Button">
            <Setter Property="Background" Value="#4b5563"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="b" Background="{TemplateBinding Background}"
                                CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="b" Property="Opacity" Value="0.85"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Border Background="#2b2d31" CornerRadius="12" Padding="24" Margin="8">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Text="错误" FontSize="18" FontWeight="SemiBold" Foreground="#f23f43" Margin="0,0,0,12"/>

            <TextBox x:Name="ErrorTextBox" Grid.Row="1" FontSize="13" Foreground="#e3e5e8"
                     Background="#1e1f22" BorderBrush="#3f4147" BorderThickness="1"
                     Padding="10" TextWrapping="Wrap"
                     IsReadOnly="True" VerticalScrollBarVisibility="Auto"
                     Margin="0,0,0,16"/>

            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="复制" Click="CopyBtn_Click" Style="{StaticResource DialogBtn}"
                        Margin="0,0,8,0"/>
                <Button Content="确定" Click="CloseBtn_Click" Style="{StaticResource DialogBtn}"
                        Background="#5865f2"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
