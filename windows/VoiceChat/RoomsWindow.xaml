<Window x:Class="VoiceChat.RoomsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="EchoLink - 房间列表" Height="560" Width="480"
        WindowStartupLocation="CenterScreen"
        Background="#f2f3f5"
        Foreground="#1e1f22"
        FontFamily="Segoe UI, Microsoft YaHei"
        ResizeMode="NoResize">

    <Window.Resources>
        <SolidColorBrush x:Key="PrimaryBrush" Color="#6366f1"/>
        <SolidColorBrush x:Key="PrimaryHoverBrush" Color="#4f46e5"/>
        <SolidColorBrush x:Key="BgInputBrush" Color="#e3e5e8"/>
        <SolidColorBrush x:Key="BgHoverBrush" Color="#d4d7dc"/>
        <SolidColorBrush x:Key="TextBrush" Color="#1e1f22"/>
        <SolidColorBrush x:Key="TextMutedBrush" Color="#6b7280"/>
        <SolidColorBrush x:Key="BorderBrush" Color="#d4d7dc"/>

        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="16,10"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}"
                                CornerRadius="10" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource PrimaryHoverBrush}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="TextButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource PrimaryBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="4"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Background="White" Margin="0" Padding="24">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <StackPanel Grid.Row="0" Margin="0,0,0,16">
                <TextBlock Text="EchoLink" FontSize="24" FontWeight="SemiBold" Foreground="{StaticResource TextBrush}"/>
                <TextBlock Text="选择房间加入，或输入房间号创建新房间" FontSize="13"
                           Foreground="{StaticResource TextMutedBrush}" Margin="0,4,0,0"/>
            </StackPanel>

            <!-- User info -->
            <Border Grid.Row="1" Background="{StaticResource BgInputBrush}" CornerRadius="10" Padding="12,10" Margin="0,0,0,16">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock x:Name="UserInfoText" FontSize="13" Foreground="{StaticResource TextBrush}" VerticalAlignment="Center"/>
                    <Button Grid.Column="1" x:Name="LogoutBtn" Content="退出登录" Style="{StaticResource TextButtonStyle}"
                            Click="LogoutBtn_Click" Visibility="Collapsed"/>
                </Grid>
            </Border>

            <!-- Join / create room input -->
            <Grid Grid.Row="2" Margin="0,0,0,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox x:Name="RoomInput" Grid.Column="0" Padding="14,12" FontSize="14"
                         Background="{StaticResource BgInputBrush}" Foreground="{StaticResource TextBrush}"
                         BorderThickness="0" VerticalContentAlignment="Center" Margin="0,0,10,0"
                         KeyDown="RoomInput_KeyDown"/>
                <Button Grid.Column="1" x:Name="JoinBtn" Content="进入" Style="{StaticResource ActionButtonStyle}"
                        Click="JoinBtn_Click"/>
            </Grid>

            <!-- Room list -->
            <Border Grid.Row="3" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="10"
                    Background="{StaticResource BgInputBrush}" Padding="12">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0" Margin="4,0,4,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="当前在线房间" FontSize="14" FontWeight="SemiBold" Foreground="{StaticResource TextBrush}" VerticalAlignment="Center"/>
                        <Button Grid.Column="1" x:Name="RefreshBtn" Content="刷新" Style="{StaticResource TextButtonStyle}"
                                Click="RefreshBtn_Click"/>
                    </Grid>

                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                        <StackPanel x:Name="RoomsPanel"/>
                    </ScrollViewer>
                </Grid>
            </Border>

            <!-- Status -->
            <TextBlock Grid.Row="4" x:Name="StatusText" FontSize="12" Foreground="{StaticResource TextMutedBrush}"
                       Margin="0,12,0,0" TextWrapping="Wrap"/>
        </Grid>
    </Border>
</Window>
